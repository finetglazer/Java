public class main_method {
    public static void main(String[] args)
    the keyword public indicates that the method can be invoked from everywhere;
    the keyword static indicates the method can be invoked without creating an instance of the class;//ko can goi ten
    the keyword void indicates the method doesn't return any value;
    the array variable args contains arguments entered at the command line,
        the array is empty if there are no arguments.
}
